<div class="search">
  <%= render 'movie_search' %><br/>
</div>

<% if @movies %>
  <ul style="list-style-type:none;">
    <% @movies.each do |movie| %>
      <li><%= link_to movie.title, "/movies/#{movie.id}" %> Vote Average: <%= movie.vote_average %></li><br>
    <% end %>
  </ul>
<% end %>
